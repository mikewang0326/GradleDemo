task hello << {
    println 'Hello World'
}

hello.doLast {
    println "Greetings from the $hello.name task."
}

// Note: if the Strings contains special symbols, like "'", "$", the brackets in the outside must use "